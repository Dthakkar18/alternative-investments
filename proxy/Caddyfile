{
  email {$EMAIL}
}

{$DOMAIN} {
  # everything that is NOT /api/* -> Next.js
  @web not path /api/*
  reverse_proxy web:3000

  # /api/* -> Django
  handle_path /api/* {
    reverse_proxy api:8000
  }

  encode zstd gzip
}

